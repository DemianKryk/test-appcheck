name: Dart

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    environment: 'prod'
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "This is a test for the secret: ${SECRET_TEST}"
        env:
          SECRET_TEST: ${{ secrets.test }}
